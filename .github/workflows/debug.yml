name: Debug 606
on:
  workflow_dispatch:
jobs:
  debug:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests beautifulsoup4 lxml python-dateutil
      - run: python debug_606b.py
      - run: |
          git config user.name "debug-bot"
          git config user.email "debug@example.com"
          git add debug_output_606b.txt
          git commit -m "debug 606b output" || echo "nothing to commit"
          git push
